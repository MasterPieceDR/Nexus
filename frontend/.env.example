# URL base del backend (sin /api al final). Si se omite, en local se usa
# http://<hostname>:8000 automáticamente.
VITE_API_URL=http://127.0.0.1:8000

# Número de WhatsApp del administrador para el botón de ayuda (solo dígitos con código de país)
VITE_ADMIN_WHATSAPP=573001234567
