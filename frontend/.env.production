# VITE_API_URL se deja vacío a propósito: api.js detecta en tiempo de ejecución
# si el sitio se sirve por CloudFront (HTTPS, mismo origen via proxy /api/*) o por
# el endpoint S3 website (HTTP directo al backend EC2). Ver services/api.js.
